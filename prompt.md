请生成一个JSON格式的数据，其中包含多个对象，每个对象描述一个关于人工智能（AI）在特定领域应用的讨论。**每个对象讨论的领域由用户输入的主题描述决定。**

每个对象应包含以下字段：

*   **title**: 讨论的标题，例如 "AI能否取代医生？"。
*   **left**: 一个包含3个元素的数组，列出AI在该领域应用的潜在优势，例如：
    *   "诊断速度快"
    *   "数据分析能力强"
    *   "减少人为错误"
*   **right**: 一个包含3个元素的数组，列出AI在该领域应用的潜在劣势，例如：
    *   "缺乏人情关怀"
    *   "误诊风险"
    *   "伦理道德问题"

**字数控制：**

*   每个对象的标题字数控制在15个字以内。
*   left和right数组中的每个元素字数控制在10个字以内。

**用户输入：**

请在system prompt的末尾提供一个主题描述，例如：

```
主题描述：医疗领域的人工智能应用
```

**示例：**

```json
[
  {
    "title": "AI能否取代医生？",
    "left": [
      "诊断速度快",
      "数据分析强",
      "减少人为错"
    ],
    "right": [
      "缺乏人情",
      "误诊风险",
      "伦理问题"
    ]
  },
  {
    "title": "AI辅助手术",
    "left": [
      "精准定位",
      "减少创伤",
      "缩短恢复期"
    ],
    "right": [
      "设备依赖",
      "操作风险",
      "成本高昂"
    ]
  }
]
```

**说明：**

*   请确保生成的JSON数据符合上述格式。
*   每个对象的标题应具有独特性，并与用户输入的主题描述相关。
*   left和right数组中的元素应具体、有针对性，并与标题所讨论的领域相关。
*   请严格遵守字数限制，确保标题不超过15个字，数组元素不超过10个字。
*   可以根据需要生成任意数量的对象。
